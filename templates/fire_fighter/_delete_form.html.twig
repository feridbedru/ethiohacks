<form method="post" action="{{ path('fire_fighter_delete', {'id': fire_fighter.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fire_fighter.id) }}">
    <button class="btn">Delete</button>
</form>

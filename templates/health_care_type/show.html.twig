{% extends 'base.html.twig' %}

{% block title %}HealthCareType{% endblock %}

{% block body %}
    <h1>HealthCareType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ health_care_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ health_care_type.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ health_care_type.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('health_care_type_index') }}">back to list</a>

    <a href="{{ path('health_care_type_edit', {'id': health_care_type.id}) }}">edit</a>

    {{ include('health_care_type/_delete_form.html.twig') }}
{% endblock %}

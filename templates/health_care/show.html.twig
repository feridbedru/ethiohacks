{% extends 'base.html.twig' %}

{% block title %}HealthCare{% endblock %}

{% block body %}
    <h1>HealthCare</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ health_care.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ health_care.name }}</td>
            </tr>
            <tr>
                <th>Points</th>
                <td>{{ health_care.points }}</td>
            </tr>
            <tr>
                <th>PhoneNumber</th>
                <td>{{ health_care.phoneNumber }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('health_care_index') }}">back to list</a>

    <a href="{{ path('health_care_edit', {'id': health_care.id}) }}">edit</a>

    {{ include('health_care/_delete_form.html.twig') }}
{% endblock %}
